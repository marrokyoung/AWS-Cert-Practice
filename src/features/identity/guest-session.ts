/**
 * Guest session continuity boundary.
 *
 * This module will manage anonymous guest session creation and resumption.
 * The client retains a lightweight session identifier for cross-visit
 * continuity. Durable study data lives on the backend, not in browser storage.
 *
 * Implementation deferred to Step 6 (frontend-api-and-session).
 */

export {};
