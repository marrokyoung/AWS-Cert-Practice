/**
 * Content loading boundary.
 *
 * This module will expose functions that load questions and concept cards
 * from the generated content catalog. UI components import from here
 * rather than reaching into raw content or generated files directly.
 *
 * Implementation deferred to Step 3 (content-pipeline).
 */

export {};
